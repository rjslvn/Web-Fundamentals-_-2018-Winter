// Biggie Size

function makeItBig(arr) {
    for (var i=0; i<arr.length; i++) {
        if (arr[i] > 0) {
            arr[i] = "big";
        }
    }
    return arr;
}

console.log(makeItBig([-1,3,5,-5]));

// Print low return high 

function lowReturnHigh() {
    var temp = 0;
    for (var i=0; i<arr.length; i++) {
        if [arr[i] > arr[i+1]] {
            temp = arr[i+1];
            arr[i+1] = a[i];
            arr[i] = temp;
        }
    }
    console.log(arr[0]);
    return arr.pop();
}

y = lowReturnHigh([1,3,-2,10,2];
console.log(lowReturnHigh([1,3,-2,10,2]));
console.log(y);